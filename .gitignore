# dependencies
node_modules

# next.js build output
.next
out

# env files — never commit credentials
.env*

# sqlite database + backups — data lives on the volume, not in git
data/
*.db
*.db-wal
*.db-shm

# misc
.DS_Store
*.pem
npm-debug.log*
*.tsbuildinfo
next-env.d.ts
